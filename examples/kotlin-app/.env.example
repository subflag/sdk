# Subflag API URL (defaults to localhost if not set)
SUBFLAG_API_URL=http://localhost:8080

# SDK API Key (required) - get this from the Subflag dashboard
# Go to Settings → Applications → Create Application
SUBFLAG_API_KEY=sdk-development-my-app-xxxxx
